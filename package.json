{
  "name": "tradetory-blog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "preview": "astro preview",
    "fetch-data": "node scripts/pipeline/collect.mjs",
    "pipeline": "node scripts/pipeline/collect.mjs && node scripts/pipeline/brief.mjs"
  },
  "dependencies": {
    "@astrojs/mdx": "^4.3.0",
    "@astrojs/rss": "^4.0.12",
    "@astrojs/sitemap": "^3.4.0",
    "astro": "^5.12.0",
    "pagefind": "^1.3.0"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
