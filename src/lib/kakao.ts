// 카카오톡 카드 공유 배선 (2026-09-01 신설).
//
// 왜 있나 — 지금 공유 줄은 `navigator.share` 를 쓴다. 모바일에서는 OS 공유 시트가 뜨고
// 그 안에 카카오톡이 있어 충분하지만, **데스크톱에는 공유 시트가 없어** 텍스트 복사로 떨어진다.
// 카카오 SDK 를 얹으면 데스크톱에서도 **히어로 이미지가 들어간 카드**로 나간다.
//
// ★ 애드센스와 같은 방식이다 — **한 줄만 채우면 켜지고, 비어 있으면 아무것도 안 나간다.**
// 심사·설정을 기다리는 동안 코드가 먼저 깔려 있어야 값이 오는 즉시 확인할 수 있다.
//
// 값은 비밀이 아니다. JavaScript 키는 **브라우저에 그대로 실리는 공개 식별자**이고
// (Supabase anon 키와 같은 성격), 카카오 콘솔에 등록한 도메인에서만 동작한다.
// 그래서 도메인 등록이 실제 자물쇠다 — 키만 있고 도메인이 없으면 아무 데서도 안 된다.
//
// 콘솔에서 마쳐야 하는 것은 **둘뿐이다**(2026-09-01 실제 동작으로 확인).
//   ① 앱 설정 → 앱 키 → **JavaScript 키**(REST API 키·Admin 키가 아니다)
//   ② **[플랫폼 키] → [JavaScript SDK 도메인]** 에 https://dotoriecon.com
// ★ 공식 문서는 「[제품 링크 관리] > [웹 도메인] 에 등록해야 메시지 공유가 켜진다」고 하지만
//   **실제로는 ②만으로 동작했다.** 콘솔 개편으로 항목이 합쳐진 것으로 보인다.
//   새 도메인을 붙일 때는 ②부터 볼 것.
// ★ **저장됐는지는 「수정 일시」로 확인한다.** 입력칸에 글자만 넣고 `+`·저장을 누르지 않으면
//   화면에는 보이는데 서버에는 없다. 그 상태에서 누르면 sharer.kakao.com 이
//   「잘못된 요청으로 인증에 실패」를 낸다(2026-09-01 에 실제로 겪었다).
// **비즈니스 앱 전환은 필요 없다.** 공유 문서의 사전 설정에 들어 있지 않다.
// (채널 메시지·친구 목록 같은 「추가 기능」에만 비즈니스 정보 심사가 붙는다.)
export const KAKAO_JS_KEY = '26a5ed4dc9a53f842dc46f0e66d1f79f';

/** 32자리 소문자 16진수인지. 오타로 잘못된 키가 전 페이지에 박히는 것을 막는다. */
export const kakaoOn = /^[0-9a-f]{32}$/.test(KAKAO_JS_KEY);

/** SDK 파일. 무결성 해시를 함께 걸어 파일이 바뀌면 로드되지 않게 한다. */
export const KAKAO_SDK_SRC = 'https://t1.kakaocdn.net/kakao_js_sdk/2.7.4/kakao.min.js';
export const KAKAO_SDK_INTEGRITY = 'sha384-DKYJZ8NLiK8MN4/C5P2dtSmLQ4KwPaoqAfyA/DfmEc1VDxu4yyC7wy6K1Hs90nka';
